#include<stdio.h>

int main() {
   double gross_salary, basic, da, ta;

   printf("Enter basic salary : ");
   scanf("%lf", &basic);

   ha = (10 * basic) / 100; // house allowance
   ca = (12 * basic) / 100; // car allowance



   printf("\nGross salary : %lf", gross_salary = basic + ha + ca);
   return 0;
}

